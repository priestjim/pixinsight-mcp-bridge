{
  "name": "pixinsight-mcp-bridge",
  "version": "1.0.0",
  "description": "MCP (Model Context Protocol) bridge for PixInsight - enables LLM interaction with PixInsight via HTTP/SSE",
  "main": "src/bridge/server.js",
  "scripts": {
    "test": "node test/run-tests.js",
    "start": "node src/bridge/server.js --standalone"
  },
  "keywords": [
    "pixinsight",
    "mcp",
    "model-context-protocol",
    "astrophotography",
    "image-processing"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=14.0.0"
  }
}
